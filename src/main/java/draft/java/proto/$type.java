package draft.java.proto;

/**
 * 
 * $class.of( $field(), $method() )
 *     .with( $anno, $body(), $expr(), $typeUse() )  
 * 
 * //$types select has a InternalSelect
 * // so that you can select a (1) Type, then
 * // the 
 * 
 * $class 
 * $interface
 * $annotation
 * $enum
 * 
 * $type.of( 
 *    $javadoc(), //TODO
 *    $package(), 
 *    $import(), 
 *    $anno(),
 *    $annos(),
 *    $modifiers(), 
 *    $id(),
 *    $typeParamter(),
 *    $typeParameters(),
 *    $implement(), //TODO
 *    $extend(),    //TODO
 * 
 *    //members
 *    $field(), 
 *    $method(),
 *    $constructor(),
 *    $staticBlock(), //TODO
 * 
 * .with( ... )
 *    $body(),
 *    $anno(),
 *    $annos(),
 *    $case(),
 *    $catch(), 
 *    $comment(),
 *    $expr(),
 *    $modifiers(), 
 *    $parameter()
 *    $parameters()
 *    $stmt()
 *    $snip()
 *    $throws()
 *    $typeParameter()
 *    $typeParameters()
 *    $typeRef()
 *    $typeUse(),
 *    $var()
 * @author Eric
 */
public class $type {
    
}
